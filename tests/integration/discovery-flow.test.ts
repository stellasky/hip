import { describe, it, expect } from 'vitest';

describe('Discovery flow', () => {
  it('selects band, shows POIs on list and map, and navigates to details', () => {
    expect(false).toBe(true); // RED
  });
});
