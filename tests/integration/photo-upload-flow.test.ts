import { describe, it, expect } from 'vitest';

describe('Photo upload flow', () => {
  it('validates size, uploads to S3, lists thumbnails, and deletes', () => {
    expect(false).toBe(true); // RED
  });
});
