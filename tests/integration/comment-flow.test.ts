import { describe, it, expect } from 'vitest';

describe('Commenting flow', () => {
  it('adds, edits, deletes comments with optimistic UI and LWW conflict resolution', () => {
    expect(false).toBe(true); // RED
  });
});
